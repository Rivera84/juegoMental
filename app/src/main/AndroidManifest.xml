<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.juegomental">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/memoria"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/memoria"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ModoDificil" />
        <activity android:name=".ModoFacil" />
        <activity android:name=".Dificultad" />
        <activity android:name=".ComoJugar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>